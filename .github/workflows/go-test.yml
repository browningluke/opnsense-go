name: Post-merge Tests (Tier 2)

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'scripts/**'
      - 'README.md'
      - '.github/**'

permissions:
  contents: read

jobs:
  test:
    uses: ./.github/workflows/go-test-reusable.yml
    with:
      packages: './...'

